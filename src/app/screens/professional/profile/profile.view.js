import React from 'react';
import { View } from 'react-native';
import proStyles from './profile.styles';

const ProfileView = (props) => {
  const styles = proStyles;

  return (
    <View style={styles.container}>
    </View>
  );
}

export default ProfileView;

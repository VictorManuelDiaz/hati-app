import React from 'react';
import { View } from 'react-native';
import meetStyles from './meetings.styles';

const MeetingsView = (props) => {
  const styles = meetStyles;

  return (
    <View style={styles.container}>
    </View>
  );
}

export default MeetingsView;

import { StyleSheet } from 'react-native';

const sosDetailStyles = StyleSheet.create({
  container: {
    backgroundColor: '#000417',
  },
  mapContainer: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    alignItems: 'center',
    justifyContent: 'flex-end',
  },
  map: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
  }
});

export default sosDetailStyles;
